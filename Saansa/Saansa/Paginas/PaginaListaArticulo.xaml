<freshMvvm:FreshBaseContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Saansa.Paginas.PaginaListaArticulo"
             xmlns:freshMvvm="clr-namespace:FreshMvvm;assembly=FreshMvvm">
    <freshMvvm:FreshBaseContentPage.ToolbarItems>
        <ToolbarItem Text="Add" Command="{Binding AddItemCommand}" />
    </freshMvvm:FreshBaseContentPage.ToolbarItems>
    <ListView ItemsSource="{Binding Articulos}" SelectedItem="{Binding ArticuloSeleccionado}">
        <ListView.ItemTemplate >
            <DataTemplate>
                <TextCell Text="{Binding NombreArticulo}" Detail="{Binding Quantity}" />
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</freshMvvm:FreshBaseContentPage>
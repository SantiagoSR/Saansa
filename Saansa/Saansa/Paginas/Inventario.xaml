<?xml version="1.0" encoding="utf-8" ?>
<fresh:FreshBaseContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Saansa.Inventario"
             xmlns:fresh="clr-namespace:FreshMvvm;assembly=Saansa"
             Title="Inventario">
    <ContentPage.Content>
        
        <StackLayout Padding="15" Spacing="5">
            
            <Label Text="Nombre Articulo"/>
            <Entry Text="{Binding ItemName}" />
            <Label Text="Barra Articulo" />
            <Entry Text="{Binding Barcode}" />
            <Label Text="Cantidad Articulo" />
            <Entry Text="{Binding ItemQuantity}" />
            <Button Text="Save" Command="{Binding SaveCommand}" />
            
        </StackLayout>
    </ContentPage.Content>
</fresh:FreshBaseContentPage>